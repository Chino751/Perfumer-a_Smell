{
  "name": "perfumeria-smell",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Catálogo y panel administrativo de Perfumería Smell con Vercel y Supabase.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/copy-static.mjs",
    "preview": "vite preview",
    "check": "node scripts/check-project.mjs",
    "verify": "npm run check && npm run build && node scripts/check-build.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.112.4"
  },
  "devDependencies": {
    "vite": "8.2.2"
  },
  "engines": {
    "node": ">=22"
  }
}
